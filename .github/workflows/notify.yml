# ================================================
# GitHub Actions 設定ファイル
# 毎日 JST 21:00 に notify.py を自動実行する
# ================================================

name: 毎日通知を送る

on:
  # スケジュール実行の設定
  schedule:
    # UTC 12:00 = JST 21:00（日本時間）
    - cron: '0 12 * * *'

  # 手動でも実行できるようにする（テスト・動作確認用）
  workflow_dispatch:

jobs:
  send-notifications:
    runs-on: ubuntu-latest

    steps:
      # ① リポジトリのコードをサーバーにダウンロード
      - name: コードを取得する
        uses: actions/checkout@v4

      # ② Python をセットアップ
      - name: Python 3.11 をセットアップする
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # ③ requirements.txt に書いてあるライブラリをインストール
      - name: 必要なライブラリをインストールする
        run: pip install -r requirements.txt

      # ④ notify.py を実行（本番の処理）
      - name: 通知スクリプトを実行する
        run: python notify.py
        env:
          LINE_CHANNEL_SECRET: ${{ secrets.LINE_CHANNEL_SECRET }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
